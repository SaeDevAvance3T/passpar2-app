<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Case cochée (vert) -->
    <item android:drawable="@drawable/nouveau_compte_checkbox_checked" android:state_checked="true" />

    <!-- Case décochée (blanc, contour blanc) -->
    <item android:drawable="@drawable/nouveau_compte_checkbox_unchecked" android:state_checked="false" />
</selector>